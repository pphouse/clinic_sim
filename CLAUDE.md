# CLAUDE.md

日本の医療グループ（診療所チェーン）経営シミュレーションゲーム。モバイル縦持ち前提。

このファイルは Claude Code が毎回読む前提で書いてある。**迷ったらここのルールが優先。**

---

## 1. 絶対に守ること

### シム核に副作用を持ち込まない

`packages/sim` は純粋 TypeScript。以下を **import してはいけない**：

- React、DOM API、`window`、`document`
- `Date.now()`、`new Date()`、`Math.random()`
- ファイルI/O、fetch

時刻はターン番号（四半期インデックス）で表す。乱数は `createRng(seed)` からのみ取得する。
この制約が崩れると、テストが再現しなくなり、セーブデータとリプレイが壊れる。

### ゴールデンテストを緑にしたまま進む

`packages/sim/test/golden/baseline.golden.json` は検証済みのシミュレーション結果 40 四半期分。
**この JSON に合わせて実装する。** 実装に合わせて JSON を書き換えるのは禁止。

数式を意図的に変える場合のみ、以下の手順を踏む：

1. `docs/spec/01-simulation-core.md` の該当式を先に書き換える
2. 変更理由を PR 本文に書く
3. golden JSON を再生成し、差分を目視で確認する

### 金額の単位は「万円」、整数で持つ

浮動小数の累積誤差を避けるため、内部表現は万円単位の `number`（小数2桁まで）。
表示時に円へ変換する。B/S の貸借は必ず一致させ、テストで検証する。

---

## 2. ディレクトリと責務

```
packages/sim/     シミュレーション核。UI を一切知らない
packages/ui/      React。sim の状態を読んで描くだけ。計算しない
docs/spec/        仕様書。実装より先に書く
docs/design/      デザイントークンと UI 規約
```

**UI で計算しない。** 「営業利益 = 売上 - 費用」を JSX の中で書かない。
sim 側に derive 関数を足して、UI はその戻り値を表示する。
理由：同じ計算が複数画面に散ると、必ずどこかがズレる。

---

## 3. ブランチ戦略

### フェーズ1（現在）：シム核 — 単一ブランチ、並列禁止

患者ストック・待ち時間・評判・収益は相互依存していて、分割すると必ず壊れる。
`main` に直接、小さくコミットする。毎コミットで `pnpm test` を通す。

**完了条件：** 40 四半期が最後まで回り、ゴールデンテストが全て緑。

### フェーズ2：画面 — git worktree で並列

シム核の型が凍ってから開始する。

```bash
git worktree add ../med-igyoku   feature/screen-igyoku
git worktree add ../med-bank     feature/screen-bank
git worktree add ../med-shikai   feature/screen-shikai
```

**鉄則：1エージェント = 1ワークツリー = 1ディレクトリ。**
同じファイルツリーで複数のエージェントを動かすと、互いの編集を上書きする。

**並列化の単位はディレクトリ境界であって、機能ではない。**
2つの作業が同じファイルを触るなら、それは並列にできない作業。

### ブランチは短命に

1画面できたら即 main へ。1週間生きているブランチは長すぎる。
コード生成が速い分、乖離も速く進む。

### 触ってよいファイルの境界

画面ブランチで作業するとき：

- 触ってよい：`packages/ui/src/screens/<自分の画面>/**`
- 触ってよい：`docs/spec/screens/<自分の画面>.md`
- **触らない：** `packages/sim/**`、`packages/ui/src/design/**`、共有コンポーネント

シム核や共有コンポーネントに変更が必要になったら、**その場で直さず issue を立てて main で対応する。**
共有物を画面ブランチで直すと、15本のブランチが同時に壊れる。

---

## 4. 実装の順序

仕様書 → テスト → 実装。この順を崩さない。

1. `docs/spec/` に何を作るか書く（人間が書くか、Claude が書いて人間が承認する）
2. 期待値をテストに落とす
3. 実装する
4. テストを通す

「とりあえず動くものを書いてから仕様を書く」をやると、
検証済みのモデルから静かにズレていく。前回それをエクセルで検出できたのは、
先に期待値があったから。

---

## 5. モバイル制約

スクリーンショット（Coffee Inc）から読み取った制約に従う。

- **縦持ち固定。** 横向きは実装しない
- **主要操作は画面下半分。** 親指の届く範囲
- **下タブは最大5個。** 6個目が必要になったら情報設計が間違っている
- **モーダルは全画面差し替え。** ポップアップにしない
- **表を横スクロールさせない。** 列が足りないなら期間切替タブで解決する
- **数字は大きく、ラベルは小さく。** 情報密度は高くてよいが、視線の優先順位を明確に

---

## 6. 用語

コードは英語、UI は日本語。対訳を統一する（勝手に別の訳語を作らない）。

| 日本語 | コード |
|---|---|
| 診療所 | clinic |
| 患者ストック | patientStock |
| 待ち時間 | waitMinutes |
| 評判 | reputation |
| 離脱率 | churnRate |
| 診察枠 | capacity |
| 実効枠 | effectiveCapacity |
| 常勤医 | doctor |
| 看護師充足率 | nurseSufficiency |
| 医局 | igyoku |
| 医局関係値 | igyokuRelation |
| 紹介会社 | agency |
| 医師会 | medicalAssociation |
| 加算 | addon |
| 診療報酬改定 | feeRevision |
| 点数指数 | feePointIndex |

---

## 7. やらないこと

- **改定の予測ゲーム化。** 検証済みで没になった方向性。改定は起きた後に適応する外部イベント
- **UI での計算**（§2 参照）
- **仕様書のない画面の実装**
- **golden JSON の書き換え**（§1 参照）
- **共有コンポーネントの画面ブランチでの変更**（§3 参照）
